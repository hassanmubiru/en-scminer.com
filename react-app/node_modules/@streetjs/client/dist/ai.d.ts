import { type StreetClientConfig } from './http.js';
/** Parse an SSE text buffer into complete `data:` payloads, returning the remainder. */
export declare function parseSseChunk(buffer: string): {
    events: string[];
    rest: string;
};
export interface ChatMessage {
    role: 'system' | 'user' | 'assistant';
    content: string;
}
/**
 * Stream an AI chat completion. Yields text tokens as they arrive. The server is
 * expected to send SSE `data:` lines; a literal `[DONE]` ends the stream.
 */
export declare function streamChat(config: StreetClientConfig, params: {
    messages: ChatMessage[];
    model?: string;
    path?: string;
    signal?: AbortSignal;
}): AsyncGenerator<string, void, unknown>;
//# sourceMappingURL=ai.d.ts.map