import type { CliContext } from '../index.js';
export declare class RegistryCommand {
    execute(ctx: CliContext): Promise<void>;
    private publish;
    private install;
    private search;
    private list;
    private fetchAndPrintSummaries;
    private resolveLatest;
    private apiBase;
    /** Encode a (possibly scoped) plugin name path-segment-by-segment, keeping the `/`. */
    private encodeName;
    private appendPaging;
    private flag;
    private csv;
    private safeDir;
    private msg;
    private printUsage;
}
//# sourceMappingURL=registry.d.ts.map