import type { CliContext } from '../index.js';
interface FeatureSpec {
    /** npm packages to install (empty = built into streetjs core). */
    packages: string[];
    /** A short wiring snippet shown after install. */
    snippet: string;
    description: string;
}
/** Friendly feature name → install plan + wiring guidance. */
export declare const FEATURES: Record<string, FeatureSpec>;
export declare class AddCommand {
    execute(ctx: CliContext): Promise<void>;
    private npmInstall;
}
export {};
//# sourceMappingURL=add.d.ts.map