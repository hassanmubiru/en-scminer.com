// src/channels/{{NAME}}Channel.ts
// {{NAME}}Channel — auto-generated by `street make:channel`.
//
// A strongly-typed channel definition that binds a typed message union to a
// room, so consumers get compile-time checking of broadcast payloads.

import { type Room, type RealtimeMessage } from '@streetjs/realtime';

/** Typed messages carried by the "{{CHANNEL}}" channel. */
export type {{NAME}}Message =
  | RealtimeMessage<{ text: string }>;

/** Strongly-typed handle factory for the "{{CHANNEL}}" channel. */
export class {{NAME}}Channel {
  /** The stable channel name this handle broadcasts to. */
  static readonly channelName = '{{CHANNEL}}' as const;

  constructor(private readonly room: Room) {}

  /** Deliver a typed {@link {{NAME}}Message} to every member of the room. */
  broadcast(message: {{NAME}}Message): Promise<void> {
    return this.room.broadcast(message);
  }
}
